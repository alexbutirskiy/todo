// full path 'projects/project' is used bicause it's calling from task controller
$("<%= '#project_' +  @project.id.to_s%>").html( "<%= j render partial: 'projects/project', locals: {project: @project}%>" );

// FIX: Can it cause async problems?
set_sortable();
set_btn_hover();